server:
  port: 8001
spring:
  application:
    name: example-cloud-business   #唯一名称
mybatis:
  mapper-locations: classpath:mapper/*.xml

logging:
  level:
    root: info
  file:
    path: chat_app_service/business